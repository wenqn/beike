# 算法单元课程设计：约瑟夫环问题的算法探究与turtle可视化

## 课程总览
- **主题**：以约瑟夫环问题为核心，探究暴力解法、数学递推解法的算法逻辑，通过Python+turtle可视化将抽象算法转化为直观图形
- **编程语言**：Python（核心库：turtle、time、math）
- **适用对象**：高一学生
- **课程时长**：2-3节课（每节课45分钟）
- **参考教材**：山东省高中信息技术《数据与计算》（算法设计与实现章节）
- **核心目标**：
  1.  理解约瑟夫环问题的建模方法，掌握暴力解法、数学递推解法的核心逻辑
  2.  能用turtle可视化算法执行过程，理解“代码→图形”的映射关系
  3.  掌握Python核心语法（列表、循环、函数、递推公式）在算法中的应用
- **设计亮点**：
  - 每节课以“情景→建模→算法→turtle可视化→互动验证”为主线，降低算法抽象性
  - 小组互动活动贴合高一认知水平，兼顾趣味性与思维性
  - 过程性评价贯穿课堂，后测试题区分“算法理解”和“语法应用”双维度
- **实施建议**：
  - 对有较好python基础的学生可按本设计用2-3课时聚焦于算法的进阶优化上。
  - 对python基础薄弱学生，建议将第1课单独扩展为2-4课时，用于理解自定义函数、循环控制、条件判断、列表等基础概念。
  - 对于零基础学生，建议开设2-3课时前导课程用于学习turtle绘图基本操作，穿插介绍循环控制、条件判断、列表等概念的初步学习。
  - turtle绘图部分可作为参考，重点讲解建模思路，并能识别出相应语句在整个代码中的位置。

## 第1课：约瑟夫环问题建模与暴力解法可视化
### 教学目标
1.  理解约瑟夫环问题的核心规则，完成问题建模（将实际情景转化为数学/代码模型）
2.  掌握暴力解法的思路（列表模拟淘汰过程），能手动模拟小规模约瑟夫环过程
3.  用turtle绘制约瑟夫环的“淘汰过程”，掌握turtle基础绘图（圆形、文字标注、颜色控制）
4.  掌握Python列表、循环、条件判断的基础应用

### 教学过程
#### 导入环节：约瑟夫环的“历史情景”（5分钟）
- **情景讲述**：“古罗马时期，约瑟夫和40名士兵被围，为避免被俘，他们约定：围成一圈，从第1人开始报数，每数到3的人就自杀，直到最后一人。约瑟夫想存活，该站在哪个位置？”
- **简化问题**：以“5人围圈，报数到2淘汰”为例，手动模拟：
  人数：1、2、3、4、5 → 淘汰顺序：2→4→1→5 → 幸存者：3
- **引导提问**：
  1.  “如何用数字/符号表示‘围圈’‘报数’‘淘汰’这三个核心规则？”
  2.  “能不能用Python代码模拟这个过程？能不能用图形直观展示淘汰顺序？”

#### 核心讲解：问题建模与暴力解法（20分钟）
##### 1. 约瑟夫环问题建模
| 实际情景       | 数学/代码模型               | 说明                     |
|----------------|----------------------------|--------------------------|
| 士兵围圈       | 列表/环形坐标               | 用列表`[0,1,2,...,n-1]`表示n个人（索引对应位置） |
| 报数淘汰       | 索引计算+元素删除          | 每数m步，删除对应索引的元素 |
| 最后幸存者     | 列表剩余最后一个元素的索引 | 最终存活位置 = 索引+1（适配日常计数） |

##### 2. 暴力解法思路（列表模拟）
- 步骤1：初始化列表，存储所有人的编号（如5人：`people = [0,1,2,3,4]`）
- 步骤2：定义起始索引`index = 0`，报数步长`m = 2`
- 步骤3：循环删除元素，直到列表长度为1：
  - 计算淘汰索引：`index = (index + m - 1) % len(people)`（取模实现“围圈”）
  - 删除该索引的元素，记录淘汰顺序
  - 重复直到剩余1人

##### 3. turtle可视化暴力解法（核心代码讲解）
```python
import turtle
import math

# 初始化海龟
t = turtle.Turtle()
t.speed(1)  # 慢速，方便观察
screen = turtle.Screen()
screen.title("约瑟夫环暴力解法可视化")

# 绘制环形人物（n人围圈，半径150像素）
def draw_people(n, radius=150):
    """绘制n个圆形代表人物，标注编号"""
    people_pos = []  # 存储每个人的坐标，用于后续标注淘汰
    angle_step = 360 / n  # 每人之间的角度
    for i in range(n):
        # 计算坐标（极坐标转直角坐标）
        angle = i * angle_step
        x = radius * math.cos(math.radians(angle))
        y = radius * math.sin(math.radians(angle))
        # 绘制圆形
        t.penup()
        t.goto(x, y - 20)  # 圆心下移20，让编号在圆上方
        t.pendown()
        t.circle(20)  # 人物圆形大小
        # 标注编号（日常计数：i+1）
        t.penup()
        t.goto(x, y + 10)
        t.write(i+1, align="center", font=("Arial", 12, "bold"))
        people_pos.append((x, y))
    return people_pos

# 模拟淘汰过程并可视化
def josephus_violent(n, m):
    people = list(range(n))  # 0~n-1表示n个人
    pos_list = draw_people(n)  # 绘制人物并获取坐标
    t.color("red")  # 淘汰标记为红色
    t.pensize(3)
    
    while len(people) > 1:
        # 计算淘汰索引
        index = (index + m - 1) % len(people) if len(people)!=n else (m-1)%n
        # 可视化淘汰：在对应位置画叉
        x, y = pos_list[people[index]]
        t.penup()
        t.goto(x - 15, y - 15)
        t.pendown()
        t.goto(x + 15, y + 15)
        t.goto(x - 15, y + 15)
        t.goto(x + 15, y - 15)
        # 删除淘汰的人
        del people[index]
        screen.update()
        turtle.delay(1000)  # 延迟1秒，观察过程
    
    # 标注幸存者
    survivor = people[0]
    x, y = pos_list[survivor]
    t.color("green")
    t.penup()
    t.goto(x, y - 30)
    t.write("幸存者", align="center", font=("Arial", 14, "bold"))
    return survivor + 1  # 转换为日常计数

# 主程序
n = 5  # 人数
m = 2  # 报数步长
index = 0  # 初始索引
survivor = josephus_violent(n, m)
print(f"{n}人围圈，报数到{m}淘汰，幸存者位置：{survivor}")
turtle.done()
```

- **关键语法讲解**：
  - 列表操作：`list(range(n))`初始化、`del people[index]`删除元素
  - 取模运算：`% len(people)`实现“围圈”（索引超出列表长度时循环）
  - turtle绘图：`math.cos/sin`计算环形坐标、`write()`标注文字、`color()`控制颜色

#### 小组互动活动1：“手动模拟+代码验证”（15分钟）
- **活动目标**：小组合作，手动模拟约瑟夫环过程，再修改代码验证结果，理解暴力解法逻辑
- **活动流程**：
  1.  分组：4人一组，每组1台电脑（预装Python环境）
  2.  任务：
     - 第一步：手动模拟“7人围圈，报数到3淘汰”，记录淘汰顺序和幸存者位置；
     - 第二步：修改代码中`n=7`、`m=3`，运行turtle可视化程序，对比手动结果是否一致；
     - 第三步：讨论“代码中`index = (index + m - 1) % len(people)`的作用”。
  3.  展示：邀请2组上台展示手动模拟过程和代码运行结果，解释不一致的原因（如手动计数错误）。
- **过程性评价**：
  - 参与度：是否全员参与手动模拟和代码修改；
  - 准确性：手动结果与代码结果是否一致，能否解释误差原因；
  - 理解度：能否说明取模运算在“围圈”中的作用。

#### 后测试题（5分钟）
##### 一、算法后测试题（3题）
1.  5人围圈，报数到3淘汰，暴力解法中第一次淘汰的索引是（  ）（列表索引从0开始）
    A. 0  B. 1  C. 2  D. 3
2.  暴力解法模拟约瑟夫环的核心逻辑是（  ）
    A. 用数学公式直接计算幸存者位置
    B. 用列表模拟围圈，循环删除报数到指定步长的元素
    C. 用递归函数计算每轮的幸存者
    D. 用字典存储每个人的淘汰状态
3.  简述暴力解法中“取模运算”的作用，以“6人围圈，报数到4淘汰”为例说明。

##### 二、语法后测试题（5题）
1.  下列代码能正确初始化包含5个元素（0-4）的列表的是（  ）
    A. `people = [1,2,3,4,5]`  B. `people = list(range(5))`  C. `people = (0,1,2,3,4)`  D. `people = {0,1,2,3,4}`
2.  执行`people = [0,1,2,3,4]; del people[2]; print(len(people))`，输出结果是（  ）
    A. 4  B. 5  C. 3  D. 2
3.  turtle库中，`t.goto(x,y)`的作用是（  ）
    A. 让海龟移动x像素，再移动y像素  B. 让海龟移动到坐标(x,y)位置
    C. 绘制从当前位置到(x,y)的直线  D. 标注文字在(x,y)位置
4.  计算`(5 + 2 - 1) % 5`的结果是（  ）
    A. 1  B. 2  C. 3  D. 4
5.  补全代码：用turtle绘制半径为20的圆形，圆心在(100,100)
    ```python
    import turtle
    t = turtle.Turtle()
    t.penup()
    __________  # 移动到圆形左下角（圆心下移20）
    t.pendown()
    __________  # 绘制圆形
    ```

### 知识总结（课堂末尾）
- **算法核心**：暴力解法通过“列表模拟围圈+循环删除元素”还原约瑟夫环淘汰过程，直观但效率随人数增加降低；
- **建模关键**：将“围圈”转化为“列表索引取模”，“报数淘汰”转化为“索引计算+元素删除”；
- **Python语法**：列表初始化/删除、取模运算、turtle坐标计算与图形绘制。

## 第2课：约瑟夫环数学递推解法与turtle进阶可视化
### 教学目标
1.  理解约瑟夫环数学递推公式的推导逻辑，掌握递推解法的建模过程
2.  用turtle可视化递推过程（标注每轮幸存者的索引变化）
3.  掌握Python函数、递归/循环实现递推公式，理解算法效率优化

### 教学过程
#### 导入环节：暴力解法的“痛点”（5分钟）
- **回顾**：上节课用暴力解法模拟5人、7人约瑟夫环很轻松，但如果是1000人、10000人呢？
- **演示**：运行暴力解法代码（n=1000，m=5），观察运行时间（约5秒）；引出问题：“有没有更高效的方法？”
- **引导提问**：
  1.  “淘汰1人后，剩余n-1人的约瑟夫环与原问题有什么关联？”
  2.  “能不能通过‘n-1人问题的解’推导‘n人问题的解’？”

#### 核心讲解：数学递推解法（20分钟）
##### 1. 递推公式推导（简化版，适配高一水平）
- 定义：`f(n,m)`表示n人围圈、报数到m淘汰的幸存者**索引**（从0开始）；
- 边界条件：`f(1,m) = 0`（只有1人时，幸存者索引为0）；
- 递推公式：`f(n,m) = (f(n-1,m) + m) % n`（n人问题的解 = n-1人问题的解 + 步长m，再取模n）；
- 示例：n=5，m=2
  - f(1,2)=0
  - f(2,2)=(0+2)%2=0
  - f(3,2)=(0+2)%3=2
  - f(4,2)=(2+2)%4=0
  - f(5,2)=(0+2)%5=2 → 幸存者位置=2+1=3（与暴力解法一致）

##### 2. 递推解法建模
| 递推步骤 | 数学计算 | 实际意义 |
|----------|----------|----------|
| f(1,m)=0 | 基础条件 | 1人时幸存者索引为0 |
| f(n,m)=(f(n-1,m)+m)%n | 递推计算 | 从n-1人问题的解推导n人问题的解 |
| 最终结果 | f(n,m)+1 | 转换为日常计数（1开始） |

##### 3. turtle可视化递推过程（核心代码讲解）
```python
import turtle
import math

# 初始化海龟
t = turtle.Turtle()
screen = turtle.Screen()
screen.title("约瑟夫环递推解法可视化")
t.speed(0)

# 绘制递推过程表格
def draw_table(n, m):
    """绘制递推表格，标注每轮f(k,m)的值"""
    # 绘制表格边框
    t.penup()
    t.goto(-300, 200)
    t.pendown()
    t.goto(200, 200)
    t.goto(200, -200)
    t.goto(-300, -200)
    t.goto(-300, 200)
    
    # 绘制列标题（人数k、f(k,m)、实际位置）
    col_titles = ["人数k", "f(k,m)", "实际位置"]
    for i, title in enumerate(col_titles):
        t.penup()
        t.goto(-300 + i*160, 180)
        t.write(title, align="center", font=("Arial", 12, "bold"))
        # 绘制竖线
        t.pendown()
        t.goto(-300 + i*160, -200)
    
    # 计算递推值并绘制每行
    f_prev = 0  # f(1,m)=0
    row_y = 150
    for k in range(1, n+1):
        if k == 1:
            f_k = 0
        else:
            f_k = (f_prev + m) % k
        # 填写每行内容
        t.penup()
        t.goto(-300 + 0*160, row_y)
        t.write(k, align="center", font=("Arial", 12, "normal"))
        t.goto(-300 + 1*160, row_y)
        t.write(f_k, align="center", font=("Arial", 12, "normal"))
        t.goto(-300 + 2*160, row_y)
        t.write(f_k + 1, align="center", font=("Arial", 12, "normal"))
        # 标注递推关系（k≥2时）
        if k >= 2:
            t.color("red")
            t.penup()
            t.goto(-300 + 1*160, row_y + 10)
            t.write(f"=({f_prev}+{m})%{k}", font=("Arial", 10, "normal"))
            t.color("black")
        # 更新上一轮值
        f_prev = f_k
        row_y -= 30
        # 绘制横线
        t.pendown()
        t.goto(-300, row_y)
        t.goto(200, row_y)
    
    # 标注最终结果
    t.color("green")
    t.penup()
    t.goto(0, -220)
    t.write(f"{n}人围圈，报数到{m}淘汰，幸存者位置：{f_prev + 1}", 
            align="center", font=("Arial", 14, "bold"))
    return f_prev + 1

# 递推解法函数
def josephus_recursive(n, m):
    if n == 1:
        return 0
    return (josephus_recursive(n-1, m) + m) % n

# 主程序
n = 5
m = 2
# 绘制递推表格
survivor = draw_table(n, m)
# 验证递归解法
recur_survivor = josephus_recursive(n, m) + 1
print(f"递推解法结果：{survivor}，递归解法结果：{recur_survivor}")
turtle.done()
```

- **关键语法讲解**：
  - 函数递归：`josephus_recursive`调用自身，实现递推公式；
  - 循环递推：用`for`循环从1到n计算每轮`f(k,m)`，避免递归栈溢出；
  - turtle表格绘制：坐标计算、文字对齐、颜色标注递推关系。

#### 小组互动活动2：“递推公式接力赛”（15分钟）
- **活动目标**：小组合作，用递推公式计算幸存者位置，再用代码验证，理解递推效率优势
- **活动流程**：
  1.  分组：3-4人一组，每组发放“递推公式计算表”（空白）
  2.  任务：
     - 第一步：接力计算“10人围圈，报数到4淘汰”的递推值（从k=1到k=10），填写表格；
     - 第二步：修改代码中`n=10`、`m=4`，运行turtle表格可视化程序，对比计算结果；
     - 第三步：分别运行暴力解法和递推解法代码（n=1000），记录运行时间，对比效率。
  3.  竞赛规则：最先完成计算且结果正确的小组获胜，分享“递推解法比暴力解法快的原因”。
- **过程性评价**：
  - 公式应用：能否正确使用递推公式计算每轮`f(k,m)`；
  - 效率认知：能否解释“递推解法只需n次计算，暴力解法需n次循环删除元素”的效率差异；
  - 代码修改：能否正确调整代码参数，验证结果。

#### 后测试题（5分钟）
##### 一、算法后测试题（3题）
1.  约瑟夫环递推公式`f(n,m)=(f(n-1,m)+m)%n`中，`%n`的作用是（  ）
    A. 保证结果为正数  B. 实现“围圈”，限制索引在0~n-1范围内
    C. 简化计算  D. 转换为日常计数
2.  计算“6人围圈，报数到3淘汰”的幸存者索引（递推法），正确的是（  ）
    A. f(6,3)=(f(5,3)+3)%6=((f(4,3)+3)%5+3)%6=1
    B. f(6,3)=(f(5,3)+3)%6=((f(4,3)+3)%5+3)%6=3
    C. f(6,3)=(f(5,3)+3)%6=((f(4,3)+3)%5+3)%6=5
    D. f(6,3)=(f(5,3)+3)%6=((f(4,3)+3)%5+3)%6=0
3.  对比暴力解法和递推解法，说明各自的适用场景（如人数规模、效率要求）。

##### 二、语法后测试题（5题）
1.  下列递归函数定义正确的是（  ）
    A. 
    ```python
    def func(n):
        if n == 1:
            return 0
        return func(n-1) + 1
    ```
    B. 
    ```python
    def func(n):
        return func(n-1) + 1
    ```
    C. 
    ```python
    def func(n):
        if n == 1:
            return 0
        func(n-1) + 1
    ```
    D. 
    ```python
    def func(n)
        if n == 1:
            return 0
        return func(n-1) + 1
    ```
2.  执行`def f(n): return n*2; print(f(f(3)))`，输出结果是（  ）
    A. 6  B. 12  C. 9  D. 3
3.  turtle库中，`t.write(text, align="center")`的`align`参数作用是（  ）
    A. 文字颜色  B. 文字字体  C. 文字对齐方式  D. 文字大小
4.  补全递推解法代码：计算n人围圈、m步长的幸存者位置
    ```python
    def josephus_iter(n, m):
        f = 0  # f(1,m)=0
        for k in range(2, n+1):
            __________  # 递推公式
        return f + 1  # 转换为日常计数
    ```
5.  执行`math.radians(180)`的结果是（  ）（π≈3.14159）
    A. 3.14159  B. 180  C. 0  D. 90

### 知识总结（课堂末尾）
- **算法核心**：递推解法通过“从n-1人推导n人”的思路，将时间复杂度从暴力解法的O(n²)优化为O(n)，效率大幅提升；
- **建模关键**：递推公式的核心是“淘汰1人后，剩余人群的位置可映射为新的约瑟夫环问题”；
- **Python语法**：递归函数定义、循环递推实现、turtle表格绘制与文字标注。

## 第3课（可选）：约瑟夫环算法优化与拓展可视化
### 教学目标
1.  理解约瑟夫环算法的效率分析方法（时间/空间复杂度）
2.  用turtle批量可视化不同规模、不同步长的约瑟夫环过程，对比算法效率
3.  拓展约瑟夫环问题（如可变步长、多轮淘汰），掌握算法迁移应用

### 教学过程
#### 导入环节：算法的“灵活应用”（5分钟）
- **回顾**：暴力解法适合小规模、递推解法适合大规模，那如果是“报数步长随轮次变化”的约瑟夫环呢？
- **拓展情景**：“10人围圈，第一轮报数到3淘汰，第二轮报数到5淘汰，第三轮及以后报数到2淘汰，求幸存者位置”；
- **引导提问**：“如何修改递推公式/暴力解法代码，适配可变步长的场景？”

#### 核心讲解：算法优化与拓展（20分钟）
##### 1. 算法效率分析（简化版，适配高一）
| 算法       | 时间复杂度 | 空间复杂度 | 适用场景               |
|------------|------------|------------|------------------------|
| 暴力解法   | O(n²)      | O(n)       | n≤1000，要求直观展示过程 |
| 递推解法（循环） | O(n)    | O(1)       | n≥1000，追求计算效率   |
| 递推解法（递归） | O(n)    | O(n)       | n≤1000，代码简洁       |

##### 2. 可变步长约瑟夫环建模与turtle可视化
```python
import turtle
import time
import math

# 可变步长的暴力解法（拓展版）
def josephus_variable_step(n, steps):
    """
    n: 人数
    steps: 步长列表，steps[i]表示第i+1轮的报数步长
    """
    people = list(range(n))
    index = 0
    # 初始化turtle
    t = turtle.Turtle()
    screen = turtle.Screen()
    screen.title("可变步长约瑟夫环可视化")
    t.speed(1)
    pos_list = draw_people(n)  # 复用第1课的draw_people函数
    
    while len(people) > 1:
        # 取当前轮次的步长（超出列表则用最后一个步长）
        round_num = n - len(people) + 1
        m = steps[round_num-1] if round_num-1 < len(steps) else steps[-1]
        # 计算淘汰索引
        index = (index + m - 1) % len(people)
        # 可视化淘汰（标注轮次和步长）
        x, y = pos_list[people[index]]
        t.color("red")
        t.penup()
        t.goto(x, y - 30)
        t.write(f"第{round_num}轮，步长{m}", align="center", font=("Arial", 10, "normal"))
        # 画叉标记淘汰
        t.goto(x - 15, y - 15)
        t.pendown()
        t.goto(x + 15, y + 15)
        t.goto(x - 15, y + 15)
        t.goto(x + 15, y - 15)
        t.penup()
        # 删除淘汰的人
        del people[index]
        screen.update()
        turtle.delay(1000)
    
    # 标注幸存者
    survivor = people[0]
    x, y = pos_list[survivor]
    t.color("green")
    t.goto(x, y - 40)
    t.write("幸存者", align="center", font=("Arial", 14, "bold"))
    return survivor + 1

# 复用第1课的draw_people函数
def draw_people(n, radius=150):
    people_pos = []
    angle_step = 360 / n
    t = turtle.Turtle()
    for i in range(n):
        angle = i * angle_step
        x = radius * math.cos(math.radians(angle))
        y = radius * math.sin(math.radians(angle))
        t.penup()
        t.goto(x, y - 20)
        t.pendown()
        t.circle(20)
        t.penup()
        t.goto(x, y + 10)
        t.write(i+1, align="center", font=("Arial", 12, "bold"))
        people_pos.append((x, y))
    return people_pos

# 主程序
n = 8
steps = [3,5,2]  # 第一轮步长3，第二轮5，后续2
start_time = time.time()
survivor = josephus_variable_step(n, steps)
end_time = time.time()
print(f"幸存者位置：{survivor}，运行时间：{end_time - start_time:.4f}秒")
turtle.done()
```

- **关键语法讲解**：
  - 时间模块：`time.time()`计算运行时间，分析算法效率；
  - 可变步长处理：用列表存储步长，轮次超出列表长度时取最后一个步长；
  - turtle拓展：标注轮次和步长，增强可视化的信息维度。

#### 小组互动活动3：“算法效率挑战赛”（15分钟）
- **活动目标**：小组合作，优化代码并对比不同场景下的算法效率，掌握算法迁移应用
- **活动流程**：
  1.  分组：2-3人一组，每组分配不同的约瑟夫环场景（如n=5000固定步长、n=1000可变步长）
  2.  任务：
     - 第一步：分别用暴力解法、递推解法（循环）实现分配的场景，记录运行时间；
     - 第二步：修改代码，用turtle绘制“运行时间-人数”折线图（x轴人数，y轴时间）；
     - 第三步：讨论“可变步长场景下，能否用递推解法优化？为什么？”。
  3.  展示：每组展示效率对比图，总结“不同场景下的算法选择策略”。
- **过程性评价**：
  - 代码优化：能否根据场景选择合适的算法，优化运行效率；
  - 可视化拓展：能否用turtle绘制效率对比图，直观展示算法差异；
  - 迁移能力：能否分析可变步长场景下递推解法的适用性。

#### 后测试题（5分钟）
##### 一、算法后测试题（3题）
1.  时间复杂度O(n²)表示（  ）（简化版解释）
    A. 算法运行时间与n的平方成正比  B. 算法运行时间是n的平方倍
    C. 算法需要n²个存储空间  D. 算法循环n²次
2.  可变步长约瑟夫环场景下，优先选择暴力解法的原因是（  ）
    A. 效率更高  B. 递推公式无法适配可变步长
    C. 代码更简洁  D. 适合大规模数据
3.  设计一个“5人围圈，奇数轮报数到2淘汰，偶数轮报数到4淘汰”的约瑟夫环场景，简述建模思路。

##### 二、语法后测试题（5题）
1.  执行`import time; print(time.time())`，输出的是（  ）
    A. 当前时间（时:分:秒）  B. 从1970年1月1日到现在的秒数
    C. 程序运行时间  D. 随机数
2.  列表`steps = [3,5,2]`，执行`steps[3] if 3 < len(steps) else steps[-1]`，结果是（  ）
    A. 3  B. 5  C. 2  D. 报错
3.  turtle绘制折线图时，`t.goto(x1,y1); t.goto(x2,y2)`的作用是（  ）
    A. 绘制从(x1,y1)到(x2,y2)的直线  B. 移动海龟到(x2,y2)，不绘制
    C. 标注文字在(x2,y2)  D. 绘制圆形在(x2,y2)
4.  补全代码：计算程序运行时间
    ```python
    import time
    start = __________
    # 待测试的代码
    end = __________
    print(f"运行时间：{end - start:.4f}秒")
    ```
5.  下列代码能正确处理“步长列表超出轮次”的是（  ）
    A. `m = steps[round_num] if round_num < len(steps) else steps[-1]`
    B. `m = steps[round_num-1] if round_num-1 < len(steps) else steps[-1]`
    C. `m = steps[round_num] if round_num-1 < len(steps) else steps[0]`
    D. `m = steps[round_num-1] if round_num < len(steps) else steps[0]`

### 知识总结（课堂末尾）
- **算法优化**：根据问题场景选择算法（固定步长选递推，可变步长选暴力，大规模选循环递推）；
- **可视化拓展**：turtle可实现“过程可视化”“数据可视化”（如效率折线图）；
- **Python语法**：time库计算运行时间、条件表达式处理可变步长、turtle折线图绘制。

## 单元知识总结
### 一、算法核心
1.  **约瑟夫环问题建模**：
    - 核心规则：围圈（索引取模）、报数（索引计算）、淘汰（元素删除/递推）；
    - 关键映射：实际位置 = 代码索引 + 1（适配日常计数习惯）。
2.  **两种核心算法**：
    - 暴力解法：列表模拟淘汰过程，直观但效率低（O(n²)），适合小规模、需展示过程的场景；
    - 递推解法：公式`f(n,m)=(f(n-1,m)+m)%n`，效率高（O(n)），适合大规模、固定步长的场景。
3.  **算法效率**：时间复杂度是衡量算法快慢的核心指标，需根据问题规模选择算法。

### 二、Python语法核心
1.  **数据结构**：列表（初始化、删除、索引）是模拟约瑟夫环的核心载体；
2.  **控制流**：循环（for/while）实现迭代计算，递归实现递推公式，条件判断处理边界/可变步长；
3.  **turtle可视化**：
    - 基础：坐标计算（极坐标转直角坐标）、图形绘制（圆形、直线）、文字标注；
    - 进阶：表格绘制、效率折线图、多维度信息标注（轮次、步长）；
4.  **辅助库**：math库（三角函数、弧度转换）、time库（计算运行时间）。

### 三、算法思维
1.  **建模思维**：将实际情景（围圈报数）转化为数学/代码模型（列表、索引、公式）；
2.  **优化思维**：从“直观模拟”到“数学推导”，追求更高的算法效率；
3.  **迁移思维**：将约瑟夫环算法思路应用到类似问题（如环形排队、淘汰游戏）。